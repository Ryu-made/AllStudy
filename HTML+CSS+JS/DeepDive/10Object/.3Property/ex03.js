var person = {
  // 프로퍼티 키는 name, 프로퍼티 값은 'Ryu'이다.
  name: 'Ryu',
  // 프로퍼티 키는 age, 프로퍼티 값은 26이다.
  age: 26,
  // 프로퍼티 키는 nation, 프로퍼티 값은 'Republic of Korea'이다.
  nation: 'Republic of Korea'
  // 마지막 프로퍼티는 쉼표를 짓지 않는다.
};


/*
프로퍼티 구성
프로퍼티 키: 빈 문자열 포함 문자열 또는 심벌 값
프로퍼티 값: 자바스크립트에서 값으로 취급하는 모든 타입의 값(함수, 객체 포함)
*/

