// 리터럴 표기법으로 new 연산자와 함께 생성자 함수를 호출하지 않는 객체 생성 방식이다.

// 객체 리터럴
const obj = {};

// 함수 리터럴
const add = function (a, b) { return a + b; };

// 배열 리터럴
const arr = [1, 2, 3];

// 정규 표현식 리터럴
const regexp = /is/ig;